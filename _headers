# Cloudflare Pages 的快取設定（GitHub Pages 會忽略這個檔案，放著不影響）
#
# sw.js 一定要能立刻拿到最新的：service worker 是靠「檔案內容變了」才知道有新版，
# 被 CDN 快取住的話，改版之後小孩那邊永遠不會跳「有新版本了」，等於白改。
# index.html 同理 —— 整個 App 就是那一個檔案。
#
# no-cache 不是「不准存」，是「用之前一定要回來問一次有沒有變」。
# 沒變的話回 304，不會重傳，所以不會變慢。

/sw.js
  Cache-Control: no-cache

/index.html
  Cache-Control: no-cache

/
  Cache-Control: no-cache

/manifest.json
  Cache-Control: no-cache
